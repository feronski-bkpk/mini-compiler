// Программа с ошибками
fn test() {
    int x = @;  // Недопустимый символ
    string s = "unterminated;  // Незавершенная строка
    float f = 123.;  // Неполное число
    y = x * 2;  // Необъявленная переменная
}