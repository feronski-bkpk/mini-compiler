# Sprint 1 (26.02.2026)

## Структура проекта и гигиена репозитория (Must)

- [x] STR-1: Инициализировать Git-репозиторий со структурой.
- [x] STR-2: Реализовать систему сборки с одной командой.
- [x] STR-3: Создать README.md с описанием, инструкциями и примером.
- [x] STR-4: Поддерживать качество кода (Should).

## Спецификация языка (Must)

- [x] LANG-1: Создать формальную спецификацию в EBNF в language_spec.md.
- [x] LANG-2: Определить список ключевых слов.
- [x] LANG-3: Определить правила для идентификаторов.
- [x] LANG-4: Определить типы литералов.
- [x] LANG-5: Перечислить операторы и разделители.
- [x] LANG-6: Специфицировать пробелы и комментарии.

## Лексический анализатор (Scanner) (Must)

- [x] LEX-1: Реализовать структуру данных Token.
- [x] LEX-2: Реализовать интерфейс Scanner.
- [x] LEX-3: Реализовать распознавание всех типов токенов.
- [x] LEX-4: Реализовать отслеживание позиции (строки/колонки).
- [x] LEX-5: Реализовать обработку ошибок.
- [x] LEX-6: Обеспечить производительность O(n) (Should).

## Тестирование и верификация (Must)

- [x] TEST-1: Написать модульные тесты для всех случаев.
- [x] TEST-2: Организовать тестовые файлы в valid/ и invalid/.
- [x] TEST-3: Определить формат вывода токенов.
- [x] TEST-4: Создать автоматизированный тестовый раннер.
- [x] TEST-5: Обеспечить покрытие 20+ валидных и 10+ невалидных тестов (Should).

## Дополнительная цель: Препроцессор (Optional)

- [x] PRE-1: Реализовать удаление комментариев.
- [x] PRE-2: Реализовать поддержку простых макросов.
- [x] PRE-3: Реализовать интерфейс препроцессора.
- [x] PRE-4: Обрабатывать граничные случаи (комментарии в строках, рекурсия).
  
  

# Sprint 2 (12.03.2026)

## Структура проекта и гигиена репозитория (Must)

- [x] STR-1: Выполнить все требования из Sprint 1 (STR-1 to STR-4).
- [x] STR-2: Интегрировать новый код в существующую структуру (папка parser/).
- [x] STR-3: Обновить README.md с новыми опциями парсера и примерами.

## Формальная спецификация грамматики (Must)

- [x] GRAM-1: Создать документ грамматики в формате EBNF/BNF.
- [x] GRAM-2: Определить компоненты грамматики (стартовый символ, нетерминалы, терминалы).
- [x] GRAM-3: Реализовать грамматику выражений с приоритетом операторов (9 уровней).
- [x] GRAM-4: Реализовать грамматику операторов (блоки, условия, циклы, объявления).
- [x] GRAM-5: Реализовать грамматику объявлений (функции, переменные, структуры).
- [x] GRAM-6: Документировать ассоциативность операторов (левая/правая/неассоциативная).

## Реализация парсера (Must)

- [x] PAR-1: Реализовать интерфейс парсера с конструктором и методом parse().
- [x] PAR-2: Реализовать рекурсивный спуск с методами для каждой грамматической конструкции.
- [x] PAR-3: Реализовать обнаружение синтаксических ошибок с локацией.
- [x] PAR-4: Реализовать lookahead (1 токен) и логику предсказания.
- [x] PAR-5: Обеспечить производительность O(n) и разумное использование памяти (Should).

## Структура данных AST (Must)

- [x] AST-1: Определить иерархию узлов AST (базовые классы и интерфейсы).
- [x] AST-2: Реализовать узлы выражений (литералы, идентификаторы, бинарные/унарные операции).
- [x] AST-3: Реализовать узлы операторов (блоки, условия, циклы, возврат).
- [x] AST-4: Реализовать узлы объявлений (функции, структуры, параметры).
- [x] AST-5: Реализовать паттерн Visitor для обхода AST (Should).

## Визуализация и вывод AST (Must)

- [x] VIS-1: Реализовать Pretty Printer для человекочитаемого текстового формата.
- [x] VIS-2: Реализовать вывод в Graphviz DOT формат для визуализации.
- [x] VIS-3: Реализовать вывод в JSON формате для машинного чтения (Should).
- [x] VIS-4: Реализовать командный интерфейс с опциями формата вывода.

## Тестирование и верификация (Must)

- [x] TEST-1: Написать модульные тесты для всех правил грамматики.
- [x] TEST-2: Организовать тестовые файлы в valid/ и invalid/ подкатегории.
- [x] TEST-3: Реализовать Golden Testing с сравнением ожидаемых AST.
- [x] TEST-4: Реализовать тесты обнаружения ошибок с проверкой сообщений.
- [x] TEST-5: Реализовать интеграционные тесты (лексер-парсер, round-trip) (Should).

## Дополнительная цель: Расширенное восстановление после ошибок (Optional)

- [x] ERR-1: Реализовать стратегии восстановления (panic mode, phrase-level).
- [x] ERR-2: Определить точки синхронизации для восстановления.
- [x] ERR-3: Реализовать метрики качества восстановления.
- [x] ERR-4: Улучшить пользовательский опыт (множественные ошибки, подсказки).

# Sprint 3 (26.03.2026)

## Структура проекта и гигиена репозитория (Must)

- [ ] STR-1: Выполнить все требования из Sprint 2 (STR-1 до STR-3).
- [ ] STR-2: Интегрировать новый код в существующую структуру (папка semantic/ с файлами analyzer, symbol_table, type_system, errors).
- [ ] STR-3: Обновить README.md с документацией семантического анализа, новыми опциями и примерами ошибок.

## Структура данных таблицы символов (Must)

- [ ] SYM-1: Реализовать интерфейс таблицы символов с операциями входа/выхода из области видимости, вставкой и поиском (локальным и глобальным).
- [ ] SYM-2: Реализовать поддержку всех уровней вложенности (глобальный, функции, блоки, структуры) с отслеживанием глубины.
- [ ] SYM-3: Реализовать хранение полной информации о символах (имя, тип, вид, локация, сигнатуры функций, поля структур).
- [ ] SYM-4: Реализовать систему представления типов (базовые типы, структуры, функции, опционально массивы) с проверкой эквивалентности.
- [ ] SYM-5: Реализовать отслеживание информации о распределении памяти (смещение, размер, выравнивание) (Should).

## Реализация семантического анализатора (Must)

- [ ] SEM-1: Реализовать интерфейс семантического анализатора с методами analyze(), get_errors(), get_symbol_table(), get_decorated_ast().
- [ ] SEM-2: Реализовать проверку объявлений (дубликаты, валидность типов, обработка опережающих ссылок).
- [ ] SEM-3: Реализовать систему проверки типов (вывод типов выражений, совместимость присваивания, правила для бинарных/унарных операторов).
- [ ] SEM-4: Реализовать проверку функций (сигнатуры вызовов, количество и типы аргументов, соответствие return).
- [ ] SEM-5: Реализовать проверку использования переменных (объявление до использования, инициализация, область видимости).
- [ ] SEM-6: Реализовать проверку управляющих конструкций (булев тип условий, изменяемость переменных цикла).

## Сообщения об ошибках и восстановление (Must)

- [ ] ERR-1: Реализовать все категории семантических ошибок (необъявленный идентификатор, дубликат, несоответствие типов, ошибки аргументов и др.).
- [ ] ERR-2: Реализовать формат сообщений с типом ошибки, локацией (файл, строка, колонка), контекстом и предложениями по исправлению.
- [ ] ERR-3: Реализовать механизм восстановления после ошибок (продолжение анализа, сохранение информации, сбор всех ошибок).
- [ ] ERR-4: Реализовать визуальный вывод ошибок с указателем на проблемное место (Should).

## Декорированное AST и вывод (Must)

- [ ] DEC-1: Реализовать декорирование AST (аннотации типов для выражений, ссылки на символы, разрешенные вызовы функций, свертка констант).
- [ ] DEC-2: Реализовать вывод декорированного AST в текстовом формате, дамп таблицы символов и сводку ошибок.
- [ ] DEC-3: Реализовать отчет о проверке с подсчетом ошибок и списком символов по областям видимости (Should).

## Тестирование и верификация (Must)

- [ ] TEST-1: Написать модульные тесты для таблицы символов, проверки типов, сигнатур функций и правил использования переменных.
- [ ] TEST-2: Организовать тестовые программы в категориях valid/ и invalid/ с подкатегориями для каждого типа проверок.
- [ ] TEST-3: Реализовать Golden Testing для сравнения дампов таблицы символов, аннотаций типов и сообщений об ошибках.
- [ ] TEST-4: Реализовать интеграционные тесты полного конвейера (лексер → парсер → семантика) (Should).
- [ ] TEST-5: Реализовать тесты точности сообщений об ошибках (локация, полезность, множественные ошибки) (Should).

## Дополнительная цель: Базовый вывод типов для констант (Optional)

- [ ] INF-1: Реализовать вывод типов для литералов (целые, float, bool, string, null).
- [ ] INF-2: Реализовать распространение типов через выражения (бинарные операции, вызовы функций, литералы массивов, условные выражения).
- [ ] INF-3: Реализовать вывод типов для переменных с ключевым словом var и инициализатором.
- [ ] INF-4: Реализовать проверку ограничений вывода (согласованность, отсутствие неоднозначности, совместимость с аннотациями).