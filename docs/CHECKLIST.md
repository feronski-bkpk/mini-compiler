# Sprint 1 (26.02.2026)

## Структура проекта и гигиена репозитория (Must)

- [x] STR-1: Инициализировать Git-репозиторий со структурой.
- [x] STR-2: Реализовать систему сборки с одной командой.
- [x] STR-3: Создать README.md с описанием, инструкциями и примером.
- [x] STR-4: Поддерживать качество кода (Should).

## Спецификация языка (Must)

- [x] LANG-1: Создать формальную спецификацию в EBNF в language_spec.md.
- [x] LANG-2: Определить список ключевых слов.
- [x] LANG-3: Определить правила для идентификаторов.
- [x] LANG-4: Определить типы литералов.
- [x] LANG-5: Перечислить операторы и разделители.
- [x] LANG-6: Специфицировать пробелы и комментарии.

## Лексический анализатор (Scanner) (Must)

- [x] LEX-1: Реализовать структуру данных Token.
- [x] LEX-2: Реализовать интерфейс Scanner.
- [x] LEX-3: Реализовать распознавание всех типов токенов.
- [x] LEX-4: Реализовать отслеживание позиции (строки/колонки).
- [x] LEX-5: Реализовать обработку ошибок.
- [x] LEX-6: Обеспечить производительность O(n) (Should).

## Тестирование и верификация (Must)

- [x] TEST-1: Написать модульные тесты для всех случаев.
- [x] TEST-2: Организовать тестовые файлы в valid/ и invalid/.
- [x] TEST-3: Определить формат вывода токенов.
- [x] TEST-4: Создать автоматизированный тестовый раннер.
- [x] TEST-5: Обеспечить покрытие 20+ валидных и 10+ невалидных тестов (Should).

## Дополнительная цель: Препроцессор (Optional)

- [x] PRE-1: Реализовать удаление комментариев.
- [x] PRE-2: Реализовать поддержку простых макросов.
- [x] PRE-3: Реализовать интерфейс препроцессора.
- [x] PRE-4: Обрабатывать граничные случаи (комментарии в строках, рекурсия).

---

# Sprint 2 (12.03.2026)

## Структура проекта и гигиена репозитория (Must)

- [x] STR-1: Выполнить все требования из Sprint 1 (STR-1 to STR-4).
- [x] STR-2: Интегрировать новый код в существующую структуру (папка parser/).
- [x] STR-3: Обновить README.md с новыми опциями парсера и примерами.

## Формальная спецификация грамматики (Must)

- [x] GRAM-1: Создать документ грамматики в формате EBNF/BNF.
- [x] GRAM-2: Определить компоненты грамматики (стартовый символ, нетерминалы, терминалы).
- [x] GRAM-3: Реализовать грамматику выражений с приоритетом операторов (9 уровней).
- [x] GRAM-4: Реализовать грамматику операторов (блоки, условия, циклы, объявления).
- [x] GRAM-5: Реализовать грамматику объявлений (функции, переменные, структуры).
- [x] GRAM-6: Документировать ассоциативность операторов (левая/правая/неассоциативная).

## Реализация парсера (Must)

- [x] PAR-1: Реализовать интерфейс парсера с конструктором и методом parse().
- [x] PAR-2: Реализовать рекурсивный спуск с методами для каждой грамматической конструкции.
- [x] PAR-3: Реализовать обнаружение синтаксических ошибок с локацией.
- [x] PAR-4: Реализовать lookahead (1 токен) и логику предсказания.
- [x] PAR-5: Обеспечить производительность O(n) и разумное использование памяти (Should).

## Структура данных AST (Must)

- [x] AST-1: Определить иерархию узлов AST (базовые классы и интерфейсы).
- [x] AST-2: Реализовать узлы выражений (литералы, идентификаторы, бинарные/унарные операции).
- [x] AST-3: Реализовать узлы операторов (блоки, условия, циклы, возврат).
- [x] AST-4: Реализовать узлы объявлений (функции, структуры, параметры).
- [x] AST-5: Реализовать паттерн Visitor для обхода AST (Should).

## Визуализация и вывод AST (Must)

- [x] VIS-1: Реализовать Pretty Printer для человекочитаемого текстового формата.
- [x] VIS-2: Реализовать вывод в Graphviz DOT формат для визуализации.
- [x] VIS-3: Реализовать вывод в JSON формате для машинного чтения (Should).
- [x] VIS-4: Реализовать командный интерфейс с опциями формата вывода.

## Тестирование и верификация (Must)

- [x] TEST-1: Написать модульные тесты для всех правил грамматики.
- [x] TEST-2: Организовать тестовые файлы в valid/ и invalid/ подкатегории.
- [x] TEST-3: Реализовать Golden Testing с сравнением ожидаемых AST.
- [x] TEST-4: Реализовать тесты обнаружения ошибок с проверкой сообщений.
- [x] TEST-5: Реализовать интеграционные тесты (лексер-парсер, round-trip) (Should).

## Дополнительная цель: Расширенное восстановление после ошибок (Optional)

- [x] ERR-1: Реализовать стратегии восстановления (panic mode, phrase-level).
- [x] ERR-2: Определить точки синхронизации для восстановления.
- [x] ERR-3: Реализовать метрики качества восстановления.
- [x] ERR-4: Улучшить пользовательский опыт (множественные ошибки, подсказки).

---

# Sprint 3 (26.03.2026)

## Структура проекта и гигиена репозитория (Must)

- [x] STR-1: Выполнить все требования из Sprint 2 (STR-1 до STR-3).
- [x] STR-2: Интегрировать новый код в существующую структуру (папка semantic/ с файлами analyzer, symbol_table, type_system, errors).
- [x] STR-3: Обновить README.md с документацией семантического анализа, новыми опциями и примерами ошибок.

## Структура данных таблицы символов (Must)

- [x] SYM-1: Реализовать интерфейс таблицы символов с операциями входа/выхода из области видимости, вставкой и поиском (локальным и глобальным).
- [x] SYM-2: Реализовать поддержку всех уровней вложенности (глобальный, функции, блоки, структуры) с отслеживанием глубины.
- [x] SYM-3: Реализовать хранение полной информации о символах (имя, тип, вид, локация, сигнатуры функций, поля структур).
- [x] SYM-4: Реализовать систему представления типов (базовые типы, структуры, функции, опционально массивы) с проверкой эквивалентности.
- [x] SYM-5: Реализовать отслеживание информации о распределении памяти (смещение, размер, выравнивание) (Should).

## Реализация семантического анализатора (Must)

- [x] SEM-1: Реализовать интерфейс семантического анализатора с методами analyze(), get_errors(), get_symbol_table(), get_decorated_ast().
- [x] SEM-2: Реализовать проверку объявлений (дубликаты, валидность типов, обработка опережающих ссылок).
- [x] SEM-3: Реализовать систему проверки типов (вывод типов выражений, совместимость присваивания, правила для бинарных/унарных операторов).
- [x] SEM-4: Реализовать проверку функций (сигнатуры вызовов, количество и типы аргументов, соответствие return).
- [x] SEM-5: Реализовать проверку использования переменных (объявление до использования, инициализация, область видимости).
- [x] SEM-6: Реализовать проверку управляющих конструкций (булев тип условий, изменяемость переменных цикла).

## Сообщения об ошибках и восстановление (Must)

- [x] ERR-1: Реализовать все категории семантических ошибок (необъявленный идентификатор, дубликат, несоответствие типов, ошибки аргументов и др.).
- [x] ERR-2: Реализовать формат сообщений с типом ошибки, локацией (файл, строка, колонка), контекстом и предложениями по исправлению.
- [x] ERR-3: Реализовать механизм восстановления после ошибок (продолжение анализа, сохранение информации, сбор всех ошибок).
- [x] ERR-4: Реализовать визуальный вывод ошибок с указателем на проблемное место (Should).

## Декорированное AST и вывод (Must)

- [x] DEC-1: Реализовать декорирование AST (аннотации типов для выражений, ссылки на символы, разрешенные вызовы функций, свертка констант).
- [x] DEC-2: Реализовать вывод декорированного AST в текстовом формате, дамп таблицы символов и сводку ошибок.
- [x] DEC-3: Реализовать отчет о проверке с подсчетом ошибок и списком символов по областям видимости (Should).

## Тестирование и верификация (Must)

- [x] TEST-1: Написать модульные тесты для таблицы символов, проверки типов, сигнатур функций и правил использования переменных.
- [x] TEST-2: Организовать тестовые программы в категориях valid/ и invalid/ с подкатегориями для каждого типа проверок.
- [x] TEST-3: Реализовать Golden Testing для сравнения дампов таблицы символов, аннотаций типов и сообщений об ошибках.
- [x] TEST-4: Реализовать интеграционные тесты полного конвейера (лексер → парсер → семантика) (Should).
- [x] TEST-5: Реализовать тесты точности сообщений об ошибках (локация, полезность, множественные ошибки) (Should).

## Дополнительная цель: Базовый вывод типов для констант (Optional)

- [x] INF-1: Реализовать вывод типов для литералов (целые, float, bool, string, null).
- [x] INF-2: Реализовать распространение типов через выражения (бинарные операции, вызовы функций, литералы массивов, условные выражения).
- [x] INF-3: Реализовать вывод типов для переменных с ключевым словом var и инициализатором.
- [x] INF-4: Реализовать проверку ограничений вывода (согласованность, отсутствие неоднозначности, совместимость с аннотациями).

---

# Sprint 4 (09.04.2026)

## Структура проекта и гигиена репозитория (Must)

* [x] STR-1: Обеспечить выполнение всех требований Спринта 3 (STR-1 – STR-3).
* [x] STR-2: Интегрировать новый код в структуру с директорией `src/ir/`.
* [x] STR-3: Обновить README.md с документацией по IR, новыми CLI-параметрами и примерами.

## Дизайн промежуточного представления (IR) (Must)

* [x] IR-1: Определить набор инструкций трехадресного кода (арифметические, логические, сравнения, память, поток управления, функции, перемещение данных).
* [x] IR-2: Реализовать поддержку типов операндов (временные переменные, переменные, литералы, метки, адреса памяти).
* [x] IR-3: Реализовать структуру базовых блоков с входным блоком, базовыми блоками, выходным блоком и графом потока управления (CFG).
* [x] IR-4: Реализовать представление функций (имя, тип возврата, параметры, локальные переменные, базовые блоки, отображение таблицы символов).
* [x] IR-5: Сохранять информацию о типах для временных переменных и операций (Should).

## Реализация генератора IR (Must)

* [x] GEN-1: Реализовать интерфейс IRGenerator с конструктором и методами generate(), get_function_ir(), get_all_ir().
* [x] GEN-2: Реализовать трансляцию выражений (литералы, переменные, арифметические выражения, вызовы функций, доступ к полям).
* [x] GEN-3: Реализовать трансляцию операторов (присваивания, объявления переменных, операторы if, циклы while, операторы return).
* [x] GEN-4: Реализовать построение потока управления (базовые блоки, инструкции переходов, PHI-узлы, правильный порядок блоков).
* [x] GEN-5: Реализовать управление переменными (выделение временных переменных, сопоставление переменных, обработка областей видимости).

## Вывод и визуализация IR (Must)

* [x] OUT-1: Реализовать текстовый дамп IR с одной инструкцией на строку, метками блоков и комментариями.
* [x] OUT-2: Реализовать вывод Graphviz для визуализации CFG с узлами, ребрами и цветовой кодировкой.
* [x] OUT-3: Реализовать вывод JSON для машиночитаемого представления IR (Should).
* [x] OUT-4: Реализовать вывод статистики IR (количество инструкций, базовых блоков, временных переменных, оценка глубины стека) (Should).

## Тестирование и верификация (Must)

* [x] TEST-1: Написать модульные тесты для трансляции выражений, потока управления, вызовов функций, доступа к переменным и построения базовых блоков.
* [x] TEST-2: Организовать тестовые файлы в `tests/ir/generation/` (expressions/, control_flow/, functions/, integration/) и `tests/ir/validation/`.
* [x] TEST-3: Реализовать эталонное тестирование (golden testing) для сравнения ожидаемого и фактического IR.
* [x] TEST-4: Реализовать проверки валидации IR (завершение блоков, допустимые метки, определенные переменные, согласованность типов) (Should).
* [x] TEST-5: Реализовать интеграционные тесты для полного конвейера исходный код → AST → IR (Should).

## Дополнительная цель: Базовая "смотровая" оптимизация (Optional)

* [x] OPT-1: Реализовать алгебраические упрощения (x + 0 → x, x * 1 → x, x * 0 → 0).
* [x] OPT-2: Реализовать свертку констант (3 + 4 → 7).
* [x] OPT-3: Реализовать снижение силы операций (x * 2 → x + x, x / 2 → x >> 1).
* [x] OPT-4: Реализовать удаление мертвого кода (удаление неиспользуемых присваиваний).
* [x] OPT-5: Реализовать сцепление переходов (JUMP L1; L1: JUMP L2 → JUMP L2).
* [x] OPT-6: Реализовать интерфейс PeepholeOptimizer с методами optimize() и get_optimization_report().
* [x] OPT-7: Обеспечить безопасность оптимизаций (сохранение семантики, отключаемость для отладки).
* [x] OPT-8: Реализовать отслеживание метрик оптимизаций (количество удаленных инструкций, сокращение временных переменных).

---

# Sprint 5 (23.04.2026)

## Структура проекта и чистота репозитория (Must)

* [x] STR-1: Выполнить все требования Спринта 4 (STR-1 до STR-3).
* [x] STR-2: Интегрировать новый код в структуру с директорией `codegen/` (x86_generator, stack_frame, register_allocator, abi) и `runtime/` (runtime.asm).
* [x] STR-3: Обновить README.md с документацией по x86-64 генерации, ABI, рантайм-библиотеке, примерам трансляции и инструкциям по ассемблированию/линковке.

## Генерация x86-64 ассемблерного кода (Must)

* [x] ASM-1: Генерировать выходной ассемблер в синтаксисе NASM/GAS с корректными директивами (`section .text`, `section .data`, `global`) и комментариями.
* [x] ASM-2: Реализовать отображение IR-инструкций в x86-64: арифметика (add, sub, imul, idiv), логические (and, or, not, xor), сравнения (cmp + set*/cmov*), память (mov), управление потоком (jmp, jnz, call, ret).
* [x] ASM-3: Корректно указывать размер операндов (eax/rax/al для 32/64/8-бит, xmm0-xmm15 для float).
* [x] ASM-4: Реализовать трансляцию функций: пролог, передача параметров по ABI, выделение локальных переменных, возвращаемое значение в rax/xmm0, эпилог.
* [x] ASM-5: Генерировать метки: функций (`function_name:`), базовых блоков (`.LBB0`), строковых литералов (`.L.str0`), глобальных переменных.

## Управление стековым фреймом (Must)

* [x] STK-1: Генерировать пролог функции: `push rbp`, `mov rbp, rsp`, `sub rsp, N` (N кратно 16).
* [x] STK-2: Реализовать передачу параметров согласно System V ABI: целочисленные → rdi, rsi, rdx, rcx, r8, r9; float → xmm0-xmm7; дополнительные аргументы в стеке справа налево.
* [x] STK-3: Реализовать выделение локальных переменных: вычисление общего объема, выравнивание стека по 16 байт перед call, назначение смещений относительно rbp.
* [x] STK-4: Генерировать эпилог функции: восстановление rsp, `pop rbp`, `ret`.
* [x] STK-5: Документировать стратегию использования красной зоны (128 байт ниже rsp) для листовых функций (Should).

## Хранение переменных и размещение в памяти (Must)

* [x] MEM-1: Назначать отрицательные смещения от rbp для локальных переменных (`[rbp-8]`), положительные для сохраненных регистров и обратного адреса (`[rbp+8]`).
* [x] MEM-2: Реализовать временное хранение: вытеснение временных переменных в стек при исчерпании регистров, повторное использование слотов.
* [x] MEM-3: Размещать глобальные переменные в соответствующих секциях: `.data` (инициализированные), `.bss` (неинициализированные), `.rodata` (только чтение), добавлять `extern` для внешних ссылок.
* [x] MEM-4: Обеспечить выравнивание стека по 16 байт при входе в функцию и естественное выравнивание для типов данных (Should).

## Рантайм-библиотека и линковка (Must)

* [x] RT-1: Реализовать рантайм-библиотеку (runtime.asm) с функциями: `print_int`, `print_string`, `read_int`, `exit` (и опционально `malloc`/`free`).
* [x] RT-2: Реализовать интерфейс системных вызовов Linux x86-64: номер в rax, аргументы в rdi/rsi/rdx/r10/r8/r9, возврат в rax.
* [x] RT-3: Поддерживать статическую линковку рантайм-библиотеки и опции для позиционно-независимого кода.
* [x] RT-4: Реализовать стартовый код (`_start`) для автономных программ с вызовом `main` и завершением через `exit` (Should).

## Тестирование и верификация (Must)

* [x] TEST-1: Проверять валидность ассемблера (возможность ассемблирования), соответствие ABI, корректность пролога/эпилога, целостность потока управления.
* [x] TEST-2: Выполнять сквозные тесты: компиляция → ассемблирование → линковка → выполнение → сравнение вывода с ожидаемым.
* [x] TEST-3: Организовать тесты в директориях: `tests/codegen/valid/` (arithmetic_ops, control_flow, function_calls, io_operations, integration) и `tests/codegen/invalid/` (assembly_errors, runtime_errors).
* [x] TEST-4: Реализовать конвейер выполнения тестов: компиляция → nasm → ld → выполнение → верификация.
* [x] TEST-5: Написать тесты соответствия ABI: сохранение регистров, выравнивание стека, передача параметров, обработка возвращаемых значений (Should).

## Дополнительная цель: Простое распределение регистров (Optional)

* [x] REG-1: Реализовать стратегию распределения регистров (фиксированное назначение, линейное сканирование, вытеснение в стек).
* [x] REG-2: Соблюдать разделение регистров на caller-saved (rax, rcx, rdx, rsi, rdi, r8-r11) и callee-saved (rbx, rsp, rbp, r12-r15).
* [x] REG-3: Реализовать алгоритм распределения (анализ времени жизни, граф конфликтов, раскраска/линейное сканирование, генерация кода вытеснения).
* [x] REG-4: Отслеживать метрики производительности: статистику использования регистров, количество вытеснений, количество инструкций до/после.

---

# Sprint 6 (07.05.2026)

## Структура проекта и гигиена репозитория (Must)

- [ ] STR-1: Выполнить все требования из Sprint 5 (STR-1 до STR-3).
- [ ] STR-2: Расширить компоненты кодогенерации (папка codegen/).
- [ ] STR-3: Обновить README.md с документацией управляющих конструкций и примерами.

## Условные операторы (Must)

- [ ] COND-1: Реализовать генерацию if statement (условие, переход, then-body, слияние).
- [ ] COND-2: Реализовать генерацию if-else statement (условие, then-body, else-body, слияние).
- [ ] COND-3: Реализовать реляционные операторы для x86-64 (jl, jle, jg, jge, je, jne, jb).
- [ ] COND-4: Обрабатывать вложенные условные операторы с уникальными метками.
- [ ] COND-5: Поддерживать типы в условных выражениях (целые, float, булевы, указатели).

## Циклы (Must)

- [ ] LOOP-1: Реализовать генерацию while loop (заголовок, условие, тело, выход, обратный переход).
- [ ] LOOP-2: Реализовать генерацию for loop (инициализация, условие, тело, обновление).
- [ ] LOOP-3: Генерировать оптимизированный код циклов (вынос инвариантов, counted loops) (Should).
- [ ] LOOP-4: Обрабатывать переменные циклов (область видимости, модификации, UB).

## Логические операторы с коротким замыканием (Must)

- [ ] LOGIC-1: Реализовать AND (&&) с short-circuit (левый false → переход к false).
- [ ] LOGIC-2: Реализовать OR (||) с short-circuit (левый true → переход к true).
- [ ] LOGIC-3: Реализовать NOT (!) через xor или инверсию сравнения.
- [ ] LOGIC-4: Обрабатывать сложные булевы выражения (вложенность, минимальные переходы).

## Генерация сложных выражений (Must)

- [ ] EXPR-1: Реализовать приоритет операторов (9 уровней: primary, unary, *, +, <, ==, &&, ||, =).
- [ ] EXPR-2: Обходить деревья выражений (пост-порядок для арифметики, in-order с short-circuit для логики).
- [ ] EXPR-3: Реализовать повышение типов (int→float, продвижение, расширение знака/нуля).
- [ ] EXPR-4: Поддерживать сложные операнды (массивы, структуры, вызовы функций, скобки).

## Тестирование и верификация (Must)

- [ ] TEST-1: Написать тесты управляющих конструкций (if, if-else, вложенные условия, while, for).
- [ ] TEST-2: Написать тесты логических операторов (short-circuit, таблицы истинности, вложенность).
- [ ] TEST-3: Написать тесты выражений (приоритет, ассоциативность, повышение типов).
- [ ] TEST-4: Организовать тестовые файлы в conditionals/, loops/, logical_ops/, complex_expressions/.
- [ ] TEST-5: Выполнять интеграционные тесты (компиляция, запуск, проверка вывода) (Should).

## Дополнительная цель: Break и Continue (Optional)

- [ ] LOOP-CTRL-1: Реализовать break statement (переход к выходу из цикла, проверка контекста).
- [ ] LOOP-CTRL-2: Реализовать continue statement (переход к обновлению/условию, проверка контекста).
- [ ] LOOP-CTRL-3: Обрабатывать область видимости при break/continue (очистка, состояние, стек).
- [ ] LOOP-CTRL-4: Реализовать switch statement (таблица переходов, default, fall-through).

---
