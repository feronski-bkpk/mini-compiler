fn factorial(int n) -> int {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

fn max(int a, int b) -> int {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

fn main() {
    int f = factorial(5);
    int m = max(10, 20);
    return f + m;
}